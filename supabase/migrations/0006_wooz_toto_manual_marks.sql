-- ============================================================
-- 토토 Z — 관리자 수동 마킹 오버라이드
-- 자동(규칙+티켓빌더) 마킹을 관리자가 정성적으로 덮어쓸 때 사용.
-- 형식: 쉼표구분 한글 마킹. 예 '승,무'(더블) / '패'(단식). null이면 자동.
-- 티켓빌더가 forced로 받아 조합수(예산)를 나머지 경기로 맞춘다.
-- ============================================================
alter table wooz_toto_matches add column if not exists manual_marks text;
